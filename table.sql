create table person (
                        id_p integer primary key,
                        name varchar (255),
                        surname varchar(255),
                        age integer,
                        address varchar(255)
);